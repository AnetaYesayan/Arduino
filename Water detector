int analogPin = A0;
void setup() {
  Serial.begin(9600); 
}
void loop() {
  // int sensorValue = analogRead(analogPin); 
  // int scaledValue = map(sensorValue, 0, 1023, 0, 255);
     Serial.println(scaledValue);
  if (sensorValue < 500 && sensorValue > 0)  { 
    Serial.println("Water detected!");
  } else {
    Serial.println("No water detected.");
  }
  delay(1000); 
}
